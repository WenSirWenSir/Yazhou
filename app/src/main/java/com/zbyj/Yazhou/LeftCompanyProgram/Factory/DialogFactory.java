package com.zbyj.Yazhou.LeftCompanyProgram.Factory;import android.app.Dialog;import android.content.Context;import android.view.Gravity;import android.view.View;import android.view.Window;import android.view.WindowManager;import android.view.animation.Animation;import android.view.animation.AnimationUtils;import android.view.animation.RotateAnimation;import android.widget.ImageView;import android.widget.TextView;import com.zbyj.Yazhou.R;public class DialogFactory {    public class RefreshDialog extends Dialog implements View.OnClickListener {        private Context mContext;        public RefreshDialog(Context context) {            super(context);            this.mContext = context;        }        public void setDialogView(int id) {            setContentView(id);        }        /**         * 配置显示的dialog         *         * @param rotateview 旋转的img 必须是IMAGE         * @param msgview    提示的字符  必须是textView         * @param msg        提示的信息         */        public void intenstDialogView(int rotateview, int msgview, String msg,boolean canClose) {            Window wd = getWindow();            WindowManager.LayoutParams params = wd.getAttributes();            ImageView img = findViewById(rotateview);            RotateAnimation rotateAnimation = new RotateAnimation(0f, 359f, Animation                    .RELATIVE_TO_SELF, 0.5f, Animation.RELATIVE_TO_SELF, 0.5F);            rotateAnimation.setDuration(1000);            rotateAnimation.setRepeatCount(-1);            img.setAnimation(rotateAnimation);//开始旋转            TextView tv = findViewById(msgview);            tv.setText(msg.toString().trim());            params.gravity = Gravity.CENTER;            wd.setAttributes(params);            setCancelable(canClose);            show();        }        @Override        public void onClick(View v) {        }    }}